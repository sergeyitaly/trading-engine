app = "trading-engine"
primary_region = "ams"

[build]
  dockerfile = "./Dockerfile"

[http_service]
  internal_port = 8000
  force_https = true
  
  [[http_service.ports]]
    handlers = ["http"]
    port = 80
  
  [[http_service.ports]]
    handlers = ["tls", "http"]
    port = 443
